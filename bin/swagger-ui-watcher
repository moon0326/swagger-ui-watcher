#!/usr/bin/env node
'use strict';

var program = require('commander');
var fs = require('fs');

program
    .version('1.0')
    .command('watch', 'watch the target directory for any changes and update the swagger UI')
    .command('build', 'build the bundle from target directory')

program.parse(process.argv);
